#pragma once
#include <set>
#include <iostream>
#include <vector>
#include <string>

std::vector<bool> MarkAllPrimeNumbers(size_t upNumber);
std::set<size_t> GeneratePrimeNumbersSet(size_t upNumber) throw(int);